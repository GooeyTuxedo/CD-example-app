name: 'Check Branch'

on:
  push:
    branches:
      - main

jobs:
  check_branch:
    runs-on: ubuntu-latest
    steps:
      - name: Check branch
        if: git rev-parse HEAD == git rev-parse staging
        run: |
          echo "ERROR: You can only merge to main from staging."
          exit 1